{% extends 'base.html.twig' %}

{% block body %}
    <h1>Salles list</h1>

    <table>
        <thead>
            <tr>
                <th>Idsalle</th>
                <th>Numsalle</th>
                <th>Nomsalle</th>
                <th>Bloc</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for salle in salles %}
            <tr>
                <td><a href="{{ path('salle_show', { 'idSalle': salle.idSalle }) }}">{{ salle.idSalle }}</a></td>
                <td>{{ salle.numSalle }}</td>
                <td>{{ salle.nomSalle }}</td>
                <td>{{ salle.bloc }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('salle_show', { 'idSalle': salle.idSalle }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('salle_edit', { 'idSalle': salle.idSalle }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('salle_new') }}">Create a new salle</a>
        </li>
    </ul>
{% endblock %}
