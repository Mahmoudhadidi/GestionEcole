{% extends 'adminnote.html.twig' %}

{% block body %}




        <br>

        <a href="{{ path('seance_index') }}"><i class="fa fa-arrow-circle-o-left"></i> retour</a>
        <br>
        <br>
        <div class="wrapper wrapper-content  animated fadeInRight article">
            <div class="row">
                <div class="col-lg-10 col-lg-offset-1">
                    <div class="ibox">
                        <div class="ibox-content">
                            <div class="pull-right">


                                {{ form_start(delete_form) }}
                                <a href="{{ path('seance_edit', { 'idSeance': seance.idSeance }) }}"><button class="btn btn-outline btn-success btn-white btn-xs" type="button">Modifier</button></a>

                                <input type="submit" class="btn btn-outline btn-danger btn-white btn-xs" value="Supprime">
                                {{ form_end(delete_form) }}




                            </div>
                            <div class="text-center article-title">

                                <h1>
                                    Seance
                                </h1>
                            </div>
                            <center>
                                <table>
                                    <tbody>

                                    <tr>
                                        <th>Durée</th>
                                        <td>&nbsp;&nbsp;{{ seance.duree }}</td>
                                    </tr>
                                    <tr>
                                        <th>Heure </th>
                                        <td>{{ seance.heure|date('H:i') }}</td>
                                    </tr>
                                    <tr>
                                        <th>Daté</th>
                                        <td>{{ seance.date|date('Y-m-d ') }}</td>
                                    </tr>
                                    </tbody>
                                </table>
                            </center>



                            <br>
                            <br>

                        </div>






                    </div>
                </div>
            </div>


        </div>

{% endblock %}
