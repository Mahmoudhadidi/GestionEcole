{% extends 'base.html.twig' %}

{% block body %}
    <h1>Evaluation edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn btn-w-m btn-success"  class="btn btn-w-m btn-warning" type="submit" value="Edit" />
    {{ form_end(edit_form) }}
<br>
    <ul>
        <li>
            <a class="btn btn-w-m btn-info" href="{{ path('eval_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input class="btn btn-w-m btn-danger" type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
